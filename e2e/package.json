{
  "name": "@qona/e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "install": "node -e \"try { require('child_process').execSync('playwright install chromium', { stdio: 'inherit' }); } catch (e) { console.log('Playwright installation skipped or failed'); }\""
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
